<h2>Welcome to SysState!</h2>
<p>&nbsp;</p>


<h2>Statistics</h2>
<h3>Environment</h3>
<p>You have ${statistics.projects} <a href="${contextPath}/project/index.html">projects</a>, which can be combined in ${statistics.environments} <a href="${contextPath}/environment/index.html">environments</a>. 
This leads to ${statistics.projectEnvironments} project/environment configurations.</p>
<p>There are in total ${statistics.instances} <a href="${contextPath}/filter/index.html">instances</a>, which have accumulated ${statistics.states} states.</p>
<p>Since this application started up, it has done ${statistics.updates} <a href="${contextPath}/queue/index.html">update requests.</a></p>
<p>&nbsp;</p>

<h3>Configuration</h3>
<p>There are ${statistics.views} <a href="${contextPath}/view/index.html">views</a> available that can be combined with ${statistics.filters} <a href="${contextPath}/filter/list.html">filters</a>.</p>