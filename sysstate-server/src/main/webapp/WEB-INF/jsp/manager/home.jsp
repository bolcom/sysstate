<h2>Welcome to SysState!</h2>
<p>&nbsp;</p>


<h2>Statistics</h2>
<h3>Environment</h3>
<p>You have ${statistics.projects} <a href="${contextPath}/project/index.html">project(s)</a>, which can be combined in ${statistics.environments} <a href="${contextPath}/environment/index.html">environment(s)</a>. 
This leads to ${statistics.projectEnvironments} project/environment configuration(s).</p>
<p>There are in total ${statistics.instances} <a href="${contextPath}/filter/index.html">instances</a>, which have accumulated ${statistics.states} state(s).</p>
<p>&nbsp;</p>

<h3>Configuration</h3>
<p>There are ${statistics.views} <a href="${contextPath}/view/index.html">view(s)</a> available that can be combined with ${statistics.filters} <a href="${contextPath}/filter/list.html">filter(s)</a>.</p>