instance.idPattern.title=ID Grouping Pattern
instance.idPattern.description=The pattern used find groups for application and environment

instance.environmentIndex.title=Environment Index
instance.environmentIndex.description=The index to use from the pattern to define the environment

instance.applicationIndex.title=Application Index
instance.applicationIndex.description=The index to use from the pattern to define the application

instance.serverUrl.title=ServerUrl
instance.serverUrl.description=The location of your Marathon Server

instance.tags.title=Tags
instance.tags.description=These tags will be set on each found instance so that you can filter on them later on

instance.connectTimeout.title=Connection Timeout
instance.connectTimeout.description=The time to wait before the connection will time out

instance.readTimeout.title=Read Timeout
instance.readTimeout.description=The time to wait before the read will time out

instance.environmentTemplate.title=Environment Template
instance.environmentTemplate.description=The template used the parse the environmentName.
